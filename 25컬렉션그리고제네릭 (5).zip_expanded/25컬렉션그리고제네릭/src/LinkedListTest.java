/*

 ■ 컬렉션 상속 관계도 (이 파일에서 다루는 부분)

                Object
                  │
                  ▼
           ┌───────────────┐
           │  Collection   │  ← 모든 컬렉션의 최상위 인터페이스
           └───────────────┘
                  │
      ┌───────────┴───────────┐
      ▼                       ▼
   List 인터페이스           Queue 인터페이스
      │                       │
      ├───────────┐           │
      ▼           ▼           ▼
┌──────────────┐ ┌─────────────────────┐
│ ArrayList    │ │     LinkedList      │
│ (클래스)      │ │      (클래스)        │
└──────────────┘ └─────────────────────┘
  List만 구현       List + Queue(+Deque)
                    여러 인터페이스를 동시 구현
                    -> 그래서 LinkedList는 List처럼도, Queue처럼도 사용할 수 있다!

------------------------------------------------------------

 ■ 클래스 상속 계층 (참고용 - 외울 필요 없음)

			Object                            클래스
			  │
			  ▼
			AbstractCollection                클래스
			  │
			  ▼
			AbstractList                      클래스
			  │
			  ├───────────────┐
			  ▼               ▼
			ArrayList      AbstractSequentialList    클래스
			                  │
			                  ▼
			               LinkedList                클래스

			ArrayList  는 implements List
			LinkedList 는 implements List, Queue, Deque

------------------------------------------------------------

 📌 핵심 포인트 한눈 요약

				✅ LinkedList
								✔ List, Queue, Deque 인터페이스 구현
								✔ 내부 구조 → 노드(Node)들을 링크(주소)로 연결한 구조
								✔ 중간/앞/뒤 삽입·삭제에 강함 (링크만 수정하면 끝)
								❌ index 조회(get)는 느림 (0번부터 링크를 따라가야 함)

				✅ ArrayList
								✔ List 인터페이스만 구현 (Queue 기능 없음)
								✔ 내부 구조 → 연속된 배열 칸
								✔ index 조회(get)가 빠름 (칸 번호로 즉시 접근)
								❌ 중간 삽입·삭제 느림 (뒤 객체들을 전부 밀거나 당겨야 함)

 ■ Queue(큐)란? (사실 그대로 이해하기)
    - 먼저 저장한 객체가 먼저 꺼내지는 구조의 자료 보관 방식이다.
      이것을 선입선출(FIFO : First In First Out)이라고 한다.
    - 한쪽 끝(뒤)으로만 넣고(offer), 반대쪽 끝(앞)에서만 꺼낸다(poll).
    - 활용 예 : 프린터 인쇄 대기 목록, 서버의 요청 처리 대기 목록
                (스레드 단원에서 배운 "스레드 풀의 작업 대기열"이 바로 큐다!)

  주제 :   Queue부모 인터페이스를 구현한 자식 LinkedList 클래스에 대해 알아보고
  		  List부모 인터페이스를 구현한 자식 LinkedList 클래스와 ArrayList클래스의 차이점도 알아보자.

*/

import java.util.Queue;       //Queue 인터페이스 불러오기 ( offer, poll,  peek 추상메소드 사용 목적 )
import java.util.LinkedList; //Queue 와 List 두 인터페이스를 모두 구현한 자식 LinkedList 클래스 불러오기 

import java.util.List;       //부모 List 인터페이스 불러오기 (업캐스팅용)
import java.util.ArrayList;  //List 인터페이스를 구현한 자식 ArrayList 클래스 불러오기 

public class LinkedListTest {
	public static void main(String[] args) {
		
		//==================================================
		//1. LinkedList를  Queue(선입 선출 구조)로 사용하기 
		//==================================================
		
		//업캐스팅을 이용한 자식 구현 LinkedList클래스의 객체 생성
		//Queue 자료형으로 받았으므로 Queue의 메소드(offer, poll, peek)만 사용하게 된다
		//(List의 get, set 등은 Queue 자료형 참조변수로는 보이지 않는다 - 업캐스팅의 효과)
		Queue   myQue = new LinkedList();
		
		/*
		 [텍스트 모델링] 생성 직후의 메모리

		 [Stack]                        [Heap]
		 myQue (Queue 타입 참조변수)      LinkedList 객체 (비어 있음)
		    │                           ┌─────────────────┐
		    └────────────▶   │ size = 0        │
		                                │ 첫 노드 주소 = 없음 │
		                                └─────────────────┘
		                                앞(꺼내는 곳)          뒤(넣는 곳)
		                                  (비어 있음)
		*/		
		
		//boolean offer(E o)  : Queue의 뒤쪽에 객체를 넣는다. 성공하면 true반환
		myQue.offer("1-자바"); //제일 먼저 들어감 -> 제일 먼저 나올 예정
		
		/*
		 [offer 1회 후]   앞▶ [ "1-자바" ] ◀뒤          size=1
		                  (혼자이므로 앞이면서 동시에 뒤)
		*/
		
		myQue.offer("2-C++");
		/*
		 [offer 2회 후]   앞▶ [ "1-자바" ]──▶[ "2-C++" ] ◀뒤     size=2
		                       먼저 온 순서대로 앞에서부터 줄을 선다
		*/
		myQue.offer("3-API");
		/*
		 [offer 3회 후]   앞▶ [ "1-자바" ]──▶[ "2-C++" ]──▶[ "3-API" ] ◀뒤     size=3
		*/
		myQue.offer("4-JSP");
		/*
		 [offer 4회 후 - 줄서기 완성]

		   앞(꺼내는 곳)                                            뒤(넣는 곳)
		    ▼                                                        ▼
		   [ "1-자바" ]──▶[ "2-C++" ]──▶[ "3-API" ]──▶[ "4-JSP" ]     size=4

		   ★ 새 객체는 항상 "뒤"에 붙고, 꺼낼 때는 항상 "앞"에서 나간다 (선입선출)
		*/
		
		//peek() 메소드가 null 아닌 동안 =  앞쪾에 꺼낼 객체가 남아 있는 동안 반복
		while( myQue.peek()  != null  ) {
			
			//poll() : 앞 쪽 객체를 꺼내서 반환하고, Queue에서 제거한다.
			System.out.println( myQue.poll() );
								  //1-자바   <- 먼저 넣은 순서 그대로 나온다 (선입선출 확인!)
								  //2-C++
								  //3-API
								  //4-JSP
		}
		/*
		 [텍스트 모델링] while문이 도는 동안 Queue의 변화 추적 (5회 판단)

		 회차  peek() 확인(제거 안 함)   poll() 꺼냄(제거!)   실행 후 Queue 상태
		 ─────────────────────────────────────────────────────────────────────
		  1    "1-자바" != null 참       "1-자바" 반환·제거     [2-C++]─▶[3-API]─▶[4-JSP]
		  2    "2-C++" != null 참        "2-C++" 반환·제거    [3-API]─▶[4-JSP]
		  3    "3-API" != null 참        "3-API" 반환·제거    [4-JSP]
		  4    "4-JSP" != null 참        "4-JSP" 반환·제거    (비어 있음)
		  5    null    != null 거짓  ──▶ while 반복 종료 (더 꺼낼 것이 없다)

		 ★ 꺼낼 때마다 "앞" 이 한 칸씩 뒤 객체로 넘어가는 것에 주목!
		*/
		
   /*
        Queue 3대 메소드 정리

    	boolean offer(E o)
    	- Queue의 뒤쪽에 객체를 넣는다(저장). 성공하면 true 반환

    	E poll()
    	- Queue의 앞쪽 객체를 꺼내서 반환한다. 꺼낸 객체는 Queue에서 제거된다.
          만일 Queue가 비어있다면 null을 반환함

		E peek()
		- Queue의 앞쪽 객체를 반환하되, Queue에서 제거하지는 않는다. (미리 확인만)
		  만약 Queue가 비어 있다면 null을 반환함

		-> poll과 peek의 차이 : 둘 다 앞쪽 객체를 반환하지만
		   poll은 제거하고, peek은 제거하지 않는다.
		   그래서 위 while문은 peek으로 "남았는지 확인"하고 poll로 "꺼내는" 구조다.
   */	
		
		System.out.println("-----------------------------------------");
		
		//=================================================================
		//2. ArrayList의 삭제 동작 : 뒤 객체들의 "이동"이 발생한다
		//================================================================
		
		//List 부모인터페이스를 implements 구현 한 LinkedList자식클래스와 ArrayList자식클래스의 배열 메모리 차이점 알아보자
		
		//ArrayList 배열 메모리 생성 후 사용
		//<> 기호 내부에 Integer클래스명을 작성해서 Integer 객체만 저장가능한 ArrayList 배열메모리를 생성하자.
		//생성자에 2를 전달했으므로 처음 칸 수는 2칸이다.
		List<Integer> arrayList = new ArrayList<Integer>(2);
		/*
		 [텍스트 모델링] 생성 직후의 메모리

		 [Stack]                          [Heap]
		 arrayList (List 타입 참조변수)     ArrayList 객체
		    │                             ┌────────────────────────┐
		    └─────────────▶   │ size = 0               │
		                                  │ elementData(연속된 칸)   │
		                                  │ [ null ][ null ]       │
		                                  │    0       1    index  │
		                                  └────────────────────────┘
		                                  capacity=2, size=0
		*/
		//ArrayList 배열 메모리의 각칸에 Integer 객체(요소) 추가 
		arrayList.add(10);   //arrayList.add( Integer.valueOf(2) );		
		/*
		 [add 1회 후]   [ 10 ][ null ]        capacity=2, size=1
		                  0      1
		*/
		
		arrayList.add(20); //오토박싱 : Integer.valueOf(20) 객체로 만들어져 1번 칸에 추가됨
		/*
		 [add 2회 후]   [ 10 ][ 20 ]          capacity=2, size=2  ★2칸이 가득 찼다!
		                  0     1
		*/	
		
		arrayList.add(30); //칸이 가득찬 상태에서 추가!
		/*
		 [텍스트 모델링] 확장의 순간 - 내부에서 3단계가 순서대로 일어난다

		   1단계 : 2 x 1.5 = 3칸짜리 새 배열을 JVM의 HEAP영역에 만든다
		           기존배열 [ 10 ][ 20 ]        새배열 [ null ][ null ][ null ]

		   2단계 : 기존 객체들을 새 배열에 전부 복사해 옮긴다
		           기존배열 [ 10 ][ 20 ]        새배열 [ 10 ][ 20 ][ null ]
		           (버려진 기존 배열은 가비지 컬렉터가 나중에 제거)

		   3단계 : 새 배열의 빈 칸에 30을 저장한다

		 [add 3회 후]   [ 10 ][ 20 ][ 30 ]    capacity=3, size=3
		                  0     1     2
		*/		
		//ArrayList 전체 배열 메모리의 각칸에 추가로 저장된 Integer 레퍼 객체들을 반환(얻어) 받아 출력
		System.out.println("ArrayList : ");
		
		//향상된 for 반복문을 이용하여 ArrayList 배열에 저장된 Integer객체 들을 반복해서 얻어 출력
		for( int i  :  arrayList ) {		
			//★ 꺼내지는 것은 Integer 객체인데 담는 변수는 기본자료형 int다?
			//  -> 오토 언박싱  : 컴파일러가 Integer객체.intValue()를 자동으로 호출해
			//			        Integer객체 속의 int 값을 꺼내 i변수에 담아 준다.  (박싱의 반대)
			System.out.println(i);
							  //10
							  //20
							  //30
		}
		
		//위 ArrayList 배열에서 1번 index 칸의 객체를 삭제하자
		//remove(1) : 매개변수가 int이므로 remove(int index)가 호출됨
		//            -> "1번 칸의 객체(20)"를 삭제 (VectorTest02에서 배운 오버로딩 구분!)
		//            (만약 20이라는 "내용"으로 지우고 싶다면 remove(Integer.valueOf(20)) 이라고
		//             직접 박싱해서 remove(Object obj)를 호출해야 한다)
		arrayList.remove(1);
		/*
		 [텍스트 모델링] 삭제 순간 ArrayList 내부에서 일어난 일 - 3단계

		   삭제 전 :  [ 10 ][ 20 ][ 30 ]      capacity=3, size=3
		                0     1     2

		   1단계 : 1번 칸의 20을 제거 대상으로 표시
		              [ 10 ][ ×× ][ 30 ]

		   2단계 : 삭제된 칸 "뒤"에 있던 객체들을 전부 한 칸씩 앞으로 복사해 이동
		              [ 10 ][ 30 ][ 30 ]   <- 2번 칸의 30이 1번 칸으로 복사됨
		                      ▲복사

		   3단계 : 마지막 칸을 비우고 size를 1 줄인다
		              [ 10 ][ 30 ][ null ]   capacity=3, size=2
		                0     1      2
		              (★capacity는 3칸 그대로! 삭제해도 칸 수는 줄지 않는다)

		   -> 삭제된 칸 뒤에 있던 객체들이 전부 한 칸씩 앞으로 이동한다.
		      만약 객체가 100만 개라면 최악의 경우 이동 작업도 수십만 번 일어난다.
		      이것이 "ArrayList는 중간 삭제가 느리다"의 정확한 이유다.
		*/
		System.out.println("------------------------------------------------------");
		
		//==========================================================================
		//3. LinkedList의 삭제 동작  :  이동 없이 "링크 수정"만 일어 난다.
		//=========================================================================
		
		//List 부모인터페이스를 구현 받은  자식 LinkedList 클래스의 배열 메모리 구조 생성해서 살펴 보자.
		List<Integer>  linkedList = new LinkedList<Integer>();
		/*
		 ★ LinkedList의 내부 구조 : 연속된 칸이 아니라 "노드(Node)"들의 연결이다

		    노드(Node) = 힙 여기저기에 흩어져 만들어지는 작은 객체로, 내부에
		                 [ 데이터 │ 다음 노드의 주소(링크) ] 를 가지고 있다.
		                 (실제 LinkedList는 이전 노드 주소도 가진 양방향 연결이지만
		                  원리 이해를 위해 다음 방향만 그린다)

		    LinkedList 객체 자신은 "첫 번째 노드가 어디 있는지 주소"만 기억한다.
		    index 번호는 "몇 번째 노드인가"를 뜻할 뿐, 연속된 칸 번호가 아니다.

		 [텍스트 모델링] 생성 직후의 메모리

		 [Stack]                            [Heap]
		 linkedList (List 타입 참조변수)      LinkedList 객체
		    │                               ┌─────────────────┐
		    └──────────────▶ 	│ size = 0        │
		                                    │ 첫 노드 주소 = 없음 │  <- 아직 노드가 하나도 없다
		                                    └─────────────────┘
		*/		
		
					linkedList.add(40);  //Integer.valueOf(40); 레퍼객체를 담은 노드가 만들어져 연결됨
					 /*
				     [add 1회 후]  LinkedList객체 ──첫노드주소──▶ [ 데이터: 40 │ 링크: 없음 ]
				                                                     0번째 노드            size=1
				    */
					linkedList.add(50); 
				    /*
				     [add 2회 후]  마지막 노드(40)의 링크가 새 노드(50)를 가리키도록 수정된다

				       LinkedList객체 ──▶ [ 40 │ ─────▶ ] ──▶ [ 50 │ 없음 ]
				                               0번째 노드                   1번째 노드      size=2
				    */
					linkedList.add(60);
				    /*
				     [add 3회 후 - 연결 완성]

				       LinkedList객체 ──▶ [ 40 │ ─────▶ ] ──▶ [ 50 │ ─────▶ ] ──▶ [ 60 │ 없음 ]
				                                 0번째 노드                     1번째 노드             2번째 노드
				                                                                      size=3
				       ★ 세 노드는 JVM의 heap영역의 연속된 위치가 아니라 여기저기 흩어져 있고
				         오직 링크(주소)로만 순서가 유지된다!
				    */
					//LinkedList의 1번째 노드 (데이터 50) 하나 삭제
					linkedList.remove(1);
			   /*
				    [텍스트 모델링] 삭제 순간 LinkedList 내부에서 일어난 일 - 2단계

				    1단계 : 1번째 노드를 찾기 위해 첫 노드부터 링크를 따라간다
				            (0번째 노드 -> 링크 따라 -> 1번째 노드 발견)

				    2단계 : 0번째 노드의 링크를 "1번째 노드"가 아니라
				            "2번째 노드"를 가리키도록 수정한다. 그게 전부다!

				     [ 데이터: 40 ]  ───────────────────▶  [ 데이터: 60 ]
				     [ 링크: 수정됨 ]                       [ 링크: 없음  ]
				         0번째 노드                            1번째 노드
				                                                  size=2

				            연결 해제(끊음) - 아무도 가리키지 않게 된
				            50 노드는 나중에 가비지 컬렉터가 제거한다
				            [ 데이터: 50 ]
				            [ 링크       ]

				    -> 객체가 100만 개여도 삭제는 링크 수정 한 번이면 끝.
				       이동(복사) 작업이 전혀 없다. 이것이 "LinkedList는
				       중간 삽입·삭제가 빠르다"의 정확한 이유다.

				    ★ ArrayList의 remove와 나란히 비교!
				       ArrayList  remove : 뒤 객체 전부 앞으로 복사 이동 (많은 작업)
				       LinkedList remove : 앞 노드의 링크 수정 한 번   (작업 1회)
				    */					
					
					System.out.println("LinkedList : ");

				    //향상된(업그레이드 된) for 반복문을 활용하여
				    //List 부모인터페이스를 구현받은 자식LinkedList에 저장된 Integer 객체를 얻어 출력
				    for( int i   : linkedList) { //오토 언박싱으로 int값을 꺼내 담는다

				    	System.out.println(i);
				    	//40
				    	//60     <- 삭제된 50은 출력되지 않는다

				    }
				    /*
				     [텍스트 모델링] 향상된 for문의 반복 추적 (링크를 따라간다!)
				       1회 : 첫 노드주소로 0번째 노드 방문 -> 40 꺼냄 -> 언박싱 -> 출력
				       2회 : 0번째 노드의 링크를 따라 다음 노드 방문 -> 60 꺼냄 -> 언박싱 -> 출력
				       3회째 : 링크가 "없음"이므로 반복 종료
				       ★ ArrayList는 "칸 번호"로 이동했지만 LinkedList는 "링크"로 이동한다
				    */					
		
	}

}
/*
위 코드들을 실행하면 ArrayList와 LinkedList에 Integer객체를 추가하고 제거하는 동작이 어떻게 다른지 확인할수 있는데..

ArrayList는 저장된 객체를 제거하면 뒤 객체들이 각각 다른 index위치 칸으로 이동(복사)이 발생하고

LinkedList는 노드가 링크(주소)로 연결되어 있기 때문에 객체를 제거할때 링크만 수정됩니다.

■ 같은 remove(1)이 두 구조에서 처리되는 과정 최종 비교 (텍스트 모델링 요약)

   ArrayList  : [ 10 ][ 20 ][ 30 ]  ──삭제──▶  [ 10 ][ 30 ][null]   뒤 객체 복사 이동 발생
                         ××            30이 앞으로 복사됨

   LinkedList : [40│─▶]─▶[50│─▶]─▶[60│없음]  ──삭제──▶  [40│─▶]────────▶[60│없음]
                                                         링크 방향만 바뀜, 이동 없음

■ 그렇다면 무엇을 언제 쓰는가? (선택 기준)

   상황                                        유리한 쪽        이유
   ─────────────────────────────────────────────────────────────────────
   index로 자주 꺼내 본다 (get 위주)            ArrayList       칸 번호로 즉시 접근
                                                               (LinkedList는 0번 노드부터
                                                                링크를 따라가야 해서 느림)
   중간/앞쪽 삽입·삭제가 자주 일어난다           LinkedList      링크 수정만으로 끝
                                                               (ArrayList는 이동 발생)
   Queue(선입선출)로 쓰고 싶다                  LinkedList      Queue 인터페이스 구현체
   ─────────────────────────────────────────────────────────────────────
   -> 실무에서는 조회가 압도적으로 많으므로 기본은 ArrayList,
      대기열(Queue)이 필요하거나 삽입·삭제가 잦은 특수한 경우에 LinkedList를 쓴다.
*/

		/*
			결론 1 : LinkedList는 List뿐 아니라 Queue, Deque도 구현하므로
			         Queue 자료형으로 받으면 선입선출(FIFO) 구조로 사용할 수 있다.
			         공식 : offer(뒤로 넣기) / poll(앞에서 꺼내며 제거) / peek(제거 없이 확인)

			결론 2 : ArrayList의 중간 삭제 = 뒤 객체들의 이동 발생 (느린 이유)
			         LinkedList의 중간 삭제 = 링크 수정 한 번 (빠른 이유)
			         반대로 index 조회는 ArrayList가 즉시 접근이라 빠르다.

			결론 3 : 향상된 for문에서 int 변수로 받으면 Integer 객체가
			         오토 언박싱(intValue 자동 호출)되어 값이 담긴다.
			         ArrayList는 칸 번호로, LinkedList는 링크를 따라 순회한다.
		*/










